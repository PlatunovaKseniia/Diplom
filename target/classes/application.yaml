spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/diplom
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  graphql:
    schema:
      printer:
        enabled: true

learning-material:
  path: "./"
